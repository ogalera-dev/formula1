package utils;

import java.text.DecimalFormat;

/**
 * Classe d'utilitats.
 */
public abstract class Formats {
    //Format pels decimals (mínim dos decimals)
    public static final DecimalFormat FORMAT_DECIMAL = new DecimalFormat("###,##0.0");
}
